name: Docker Image CI

on:
  push:
    branches: [ main ]

jobs:

  build-image:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag venkatesan06/my-app:1.0.0
